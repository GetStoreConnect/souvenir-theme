{%- default height: default -%}
{%- default width: default -%}
{%- default style_class: default -%}

<svg{% if style_class != blank %} class="{{ style_class }}"{% endif %}{% if width != blank %} width="{{ width }}"{% endif %}{% if height != blank %} height="{{ height }}"{% endif %} viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
  <circle cx="12" cy="8" r="5"/>
  <path d="M20 21a8 8 0 0 0-16 0"/>
</svg>
