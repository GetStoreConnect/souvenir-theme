{%- default height: nil -%}
{%- default width: nil -%}
{%- default style_class: nil -%}

<svg{% if style_class != nil %} class="{{ style_class }}"{% endif %}{% if width != nil %} width="{{ width }}"{% endif %}{% if height != nil %} height="{{ height }}"{% endif %} viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
  <circle cx="12" cy="8" r="5"/>
  <path d="M20 21a8 8 0 0 0-16 0"/>
</svg>
