{%- default width: blank -%}
{%- default height: blank -%}
{%- default style_class: blank -%}

<svg{% if height != blank %} height="{{ height }}"{% endif %}{% if width != blank %} width="{{ width }}"{% endif %}{% if style_class != blank %} class="{{ style_class }}"{% endif %} viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
  <path d="m18 15-6-6-6 6"/>
</svg>